export const DEFAULT_PAGE_DIR = 'src/views';

export const DEFAULT_EXCLUDE_PAGE_DIRS = ['components'];

export const DEFAULT_DTS = 'src/typings/router-page.d.ts';

export const DEFAULT_PATTERNS = ['index.vue'];

export const DEFAULT_IGNORE_DIR_PREFIX = '_';

export const ROOT_ROUTE = 'root';

export const NOT_FOUND_ROUTE = 'not-found';
